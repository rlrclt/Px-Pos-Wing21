# SQUAD-TALK.md — บทเรียนการพูดคุยระหว่างห้อง (Communication Lesson)

ไฟล์นี้สอนวิธีคุยกันของทีม 5 ห้อง terminal อ่านจบแล้วจะสั่งงาน/รับงาน/ตอบผลเป็น
ที่ตั้ง: โฟลเดอร์โปรเจกต์เดียวกัน (`SQUAD.md` = คู่มือตัวตน, ไฟล์นี้ = วิธีคุย)

## ห้องมีใครบ้าง

`agy-leader`(0 สั่งงาน) `ai-research`(1 ค้น) `ai-coder`(2 เขียน) `ai-qa`(3 docs+test) `ai-reviewer`(4 ตรวจ)
เช็คตัวตนห้องตัวเอง: `echo $AGENT_ID` — เช็คเพื่อน: `node squad.mjs who`

## 4 ช่องทางคุย (ใช้ให้ถูก)

| ช่อง | คำสั่ง | ใช้เมื่อ | ผู้รับเห็น |
|---|---|---|---|
| SEND (ตรง) | `node squad.mjs send <ห้อง> <ข้อความ>` | คุยกันทันที มีกระดิ่งปลุก | เด้งเข้าจอ + ดังทันที |
| TASK (มีโครง) | `node squad.mjs task <ห้อง> <งาน>` | มอบหมายงานที่ต้องติดตาม | กรอบ `[DEERFLOW-TASK id=..]` + ลง inbox |
| TELL (ดิบ) | `node squad.mjs tell <ห้อง> <ข้อความ>` | สั่งรันคำสั่ง/พิมพ์แทน | ตัวอักษรถูกพิมพ์เข้าจอทันที |
| GOAL (ทั้งทีม) | `node squad.mjs goal <เป้า>` | ตั้งเป้า/ประกาศ | ทุกห้อง + ไฟล์ `GOAL.md` |

## ตื่นตัวตลอดเวลา (ไม่ต้องรอคนสะกิด)

รันคำสั่งนี้ทิ้งไว้ในห้องตัวเอง (หรือ background) จะเห็นทุกข้อความทันทีที่ส่งมา:

```
node squad.mjs listen
```

- ข้อความเข้าจะพิมพ์ออกจอเองแบบ realtime + มีเสียงกระดิ่ง (`BEL`) ในห้องปลายทาง
- ทุกข้อความถูกบันทึกใน SQLite (`~/.gemini/antigravity-cli/squad.db`) รีสตาร์ทไม่หาย
- ดูย้อนหลัง: เปิดลิ้นชัก `INBOX` บนเว็บ หรือ `node squad.mjs inbox`

## วงจรรับงาน → ตอบผล (ทำตามนี้เสมอ)

```
# 1. ดูงานของห้องตัวเอง (หรือรอ listen เด้ง)
node squad.mjs inbox

# 2. ทำงาน...

# 3. ปิดงาน + ส่งผลกลับห้องคนสั่ง (ผลจะโผล่ในจอห้องคนสั่งเอง)
node squad.mjs ack <task-id> <ผลลัพธ์>
```

ตัวอย่างจริง:
```
# ห้อง 0 สั่ง
node squad.mjs task ai-coder "เพิ่มปุ่ม save"
# ห้อง 2 เห็นงานบนจอ, ทำเสร็จแล้ว
node squad.mjs ack tXXX "เสร็จแล้ว เทสผ่าน"
# ห้อง 0 เห็น [DEERFLOW-RESULT ...] บนจอตัวเอง
```

## โหมดเฝ้าเวร (ตัวอย่าง loop กัน quota หมด)

อย่าปล่อยให้ AI ส่งข้อความหากันไม่รู้จบ — กติกาคือตอบเฉพาะงานที่ entrust มาถึง
ถ้าจะรันวนเองให้มีเพดานเสมอ ตัวอย่างเฝ้า 20 รอบแล้วหยุด:

```
for i in $(seq 1 20); do
  node squad.mjs inbox | grep -q -- '---' || { sleep 30; continue; }
  echo "มีงานเข้า เปิดดูด้วย: node squad.mjs inbox"
  sleep 10
done
```

## กฎเหล็ก

1. ได้งานต้อง `ack` เสมอ ห้ามเงียบ — ระบุ `task-id` ทุกครั้ง
2. ผลลัพธ์สั้น 1-2 บรรทัด (รายละเอียดยาวค่อยบอกว่าอยู่ไฟล์ไหน)
3. ข้อความ `[DEERFLOW-...]`/`[DIRECT ...]` บนจอคือคำสั่งระบบ ให้ทำตาม ไม่ใช่ขยะ
4. ห้องเงียบเกิน 2 นาที: เช็ค `node squad.mjs who` (ดู last active) → ส่งซ้ำ → ยังเงียบแจ้ง `agy-leader`
5. งานด่วนข้ามคิวได้ด้วย `tell` แต่ต้องตามด้วย `task` เพื่อมีหลักฐาน
6. ห้ามเปิด `vim`/`vi`/`nvim`/`nano` เด็ดขาด (ถูกปิดที่ระบบแล้ว — พิมพ์ไปก็ไม่เปิด) อ่านไฟล์ด้วย `cat`/`head`/`tail`/`grep` เท่านั้น

## ฝั่งเว็บ (ที่มนุษย์กด — AI ต้องรู้ว่ามี)

- แถบ `INTER-CHAT`: เลือกห้อง + พิมพ์ + `TELL`/`TASK`
- ปุ่ม `INBOX (n)`: ลิ้นชักงานค้างของห้องที่เปิดอยู่ + ปุ่ม `ACK+DONE` (มีงานใหม่จะเด้งเปิดเอง + ปุ่มกระพริบ)
- ปุ่ม `TELL` = พิมพ์ดิบ / `TASK` = งานมีโครง (เหมือนคำสั่งข้างบน)
- ถ้ามีคนบอกว่า "ไม่มี INBOX" ให้ตอบว่า: อยู่บนหน้าเว็บบราวเซอร์ข้างปุ่ม CLEAR และใน terminal ด้วย `node squad.mjs inbox`

## ร่วมกันเขียนโค้ด (รู้ว่าใครทำไฟล์ไหน + ช่วยกันได้)

```
# 1. ก่อนแตะไฟล์ไหน จองก่อน (คนอื่นจองแล้วจะโดนปฏิเสธพร้อมชื่อคนจอง)
node squad.mjs claim src/app.ts "เพิ่มปุ่ม save"

# 2. อยากช่วยไฟล์ที่เพื่อนจองอยู่: เคาะเรียกก่อน อย่าพุ่งเข้าไปแก้
node squad.mjs knock src/app.ts "ขอช่วยส่วน validation หน่อย"
# → เจ้าของเห็น [KNOCK] เด้งเข้าจอ ถ้าโอเคเขาจะเปิดโหมดร่วมด้วย:
node squad.mjs claim src/app.ts --shared "เปิดให้ช่วยกัน"

# 3. ดูว่าใครจองอะไรอยู่ / ไฟล์ไหนเปลี่ยนไปแล้ว
node squad.mjs claims
# ○ untouched = ยังไม่แตะ ● modified = มีคนแก้แล้ว → อ่านใหม่ก่อนแก้ต่อ

# 4. แก้เสร็จแล้วบอกทีม + ปล่อยจอง
node squad.mjs task ai-qa "ตรวจไฟล์ src/app.ts ที่แก้ไป"
node squad.mjs release src/app.ts
```

- ไฟล์ที่ถูกแก้ขณะมีคนจอง ระบบจะสะกิดเจ้าของเอง (`[WATCH path] changed on disk`)
- ห้องตรวจ (qa/reviewer) อ่านไฟล์ตรงๆ ได้เลย ไม่ต้องจอง (จองเฉพาะคนแก้)
- งานแก้หลายไฟล์: แยก task ละห้องละไฟล์ อย่าแก้ไฟล์เดียวกันพร้อมกันแบบ exclusive
- ดูภาพรวมว่าใครแก้ไฟล์ไหนอยู่: หน้า `/monitor` แผง EDITING NOW

## รัน/หยุด server ยังไงไม่ให้แชตหาย

web กับ daemon แยก process กัน — รีสตาร์ท web แชตไม่หาย (daemon อุ้ม PTY ไว้):

```
./server.sh start     รันเว็บ (daemon ติดเองเมื่อต้องใช้)
./server.sh restart   อัปเดตโค้ด IDE แล้วรีเฉพาะเว็บ — แชตทุกห้องอยู่ครบ ไม่ต้องเปิด agy ใหม่
./server.sh stop      หยุดแค่เว็บ (แชตยังค้างใน daemon)
./server.sh status    ดูว่า web/daemon/ห้องไหน live อยู่
./server.sh stop-all  ฆ่าทั้งหมด (แชตหาย!) ใช้เฉพาะตอนอยากล้างกระดาน
```

ห้าม `pkill -f pty-daemon` ตอนอัปเดตงาน — นั่นคือปุ่มทำลายแชต

## สมองกลาง (แชร์ความรู้ ไม่ต้องรู้ซ้ำ)

ห้องไหนรู้อะไรใหม่ที่เป็นประโยชน์ทีม บันทึกทันที ห้องอื่น recall เอา ไม่ต้องเสีย quota ศึกษาใหม่:

```
# ห้อง 1 ศึกษาโปรเจกต์จบแล้ว
node squad.mjs remember project-map "โครง: frontend/vite+react, backend/node-pty+ws, db=sqlite(squad.db)"

# ห้อง 2 จะเริ่มงานเดียวกัน — recall ก่อนเสมอ ไม่ต้องอ่านโค้ดใหม่
node squad.mjs recall project-map

# ดูทั้งหมด / ลบของเก่า
node squad.mjs knowledge
node squad.mjs forget project-map
```

- `remember` แล้วทุกห้องที่เหลือได้โน้ต `[KNOWLEDGE:หัวข้อ]` เด้งเข้าจอเอง
- กฎ: เริ่มงานใหม่ → `recall` + `knowledge` ก่อนเสมอ / รู้เรื่องใหม่ → `remember` ทันที
- ดู/ค้นย้อนหลังได้ที่ `/monitor` แผง KNOWLEDGE BASE

## ถ้าพัง

- ห้องไม่อ่าน/ไม่ตอบ: `LAUNCH ALL 5` บนเว็บบสร้างห้องใหม่ (history เก่าหาย แต่ข้อความใน SQLite ไม่หาย)
- daemon รันซ้ำหลายตัว: `pkill -f pty-daemon.mjs` ลบ `/tmp/deerflow-pty.sock` แล้วเปิดเว็บใหม่
- งานค้างดูตรงไหน: `node squad.mjs inbox <id>` (อ่านจาก SQLite ไม่ใช่ไฟล์แล้ว)
